/* =========================================
   TECHNOVA SOLUTIONS
   MAIN WEBSITE CSS
========================================= */

@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: 'Poppins', sans-serif;
    background: #f7f8ff;
    color: #202033;
    line-height: 1.7;
}

/* =========================================
   NAVBAR
========================================= */

.header {
    background: rgba(255, 255, 255, 0.96);
    box-shadow: 0 5px 25px rgba(0, 0, 0, 0.07);
    position: sticky;
    top: 0;
    z-index: 1000;
}

.navbar {
    max-width: 1200px;
    margin: auto;
    padding: 18px 25px;

    display: flex;
    align-items: center;
    justify-content: space-between;
}

.logo {
    color: #17172b;
    text-decoration: none;
    font-size: 28px;
    font-weight: 800;
}

.logo span {
    color: #6c4df6;
}

.nav-links {
    display: flex;
    align-items: center;
    gap: 22px;
}

.nav-links a {
    color: #333;
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    transition: 0.3s;
}

.nav-links a:hover,
.nav-links a.active {
    color: #6c4df6;
}

/* Theme button */

.theme-btn {
    width: 40px;
    height: 40px;
    border: none;
    border-radius: 50%;
    background: #eeeaff;
    cursor: pointer;
    font-size: 18px;
    transition: 0.3s;
}

.theme-btn:hover {
    transform: rotate(20deg) scale(1.1);
}

/* Mobile menu */

.menu-toggle {
    display: none;
    border: none;
    background: #6c4df6;
    color: white;
    font-size: 23px;
    width: 42px;
    height: 42px;
    border-radius: 8px;
    cursor: pointer;
}

/* =========================================
   HERO SECTION
========================================= */

.hero {
    min-height: 680px;
    padding: 90px 7%;

    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    align-items: center;
    gap: 60px;

    background:
        radial-gradient(
            circle at 10% 20%,
            rgba(108, 77, 246, 0.25),
            transparent 35%
        ),
        radial-gradient(
            circle at 90% 80%,
            rgba(0, 198, 255, 0.18),
            transparent 35%
        ),
        linear-gradient(135deg, #f8f7ff, #eef5ff);
}

.hero-content {
    max-width: 650px;
}

.hero-subtitle {
    color: #6c4df6;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 2px;
    margin-bottom: 15px;
}

.hero h1 {
    font-size: 55px;
    line-height: 1.2;
    font-weight: 800;
    margin-bottom: 25px;
}

.hero h1 span {
    color: #6c4df6;
}

.hero-content > p:not(.hero-subtitle) {
    color: #666;
    font-size: 17px;
    max-width: 600px;
    margin-bottom: 30px;
}

/* =========================================
   BUTTONS
========================================= */

.btn {
    display: inline-block;
    padding: 13px 28px;
    border-radius: 30px;
    text-decoration: none;
    font-weight: 600;
    transition: 0.3s;
}

.primary-btn {
    color: white;
    background: linear-gradient(135deg, #6c4df6, #8b68ff);
    box-shadow: 0 10px 25px rgba(108, 77, 246, 0.25);
}

.primary-btn:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 30px rgba(108, 77, 246, 0.35);
}

.secondary-btn {
    color: #6c4df6;
    background: white;
    border: 2px solid #6c4df6;
    margin-left: 10px;
}

.secondary-btn:hover {
    color: white;
    background: #6c4df6;
    transform: translateY(-4px);
}

/* =========================================
   CODE CARD
========================================= */

.hero-image {
    display: flex;
    justify-content: center;
}

.code-card {
    width: 420px;
    min-height: 300px;

    border-radius: 20px;
    padding: 0;

    overflow: hidden;

    background: #17172b;

    box-shadow:
        0 25px 60px rgba(40, 30, 100, 0.25);

    transform: rotate(2deg);
    transition: 0.4s;
}

.code-card:hover {
    transform: rotate(0deg) translateY(-10px);
}

.code-header {
    height: 48px;
    background: #252541;

    display: flex;
    align-items: center;
    gap: 8px;
    padding-left: 20px;
}

.code-header span {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: #6c4df6;
}

.code-content {
    padding: 35px;
    font-family: monospace;
    font-size: 17px;
    color: #d9d9ef;
}

.code-content p {
    margin-bottom: 15px;
}

.code-content span {
    color: #8d7aff;
}

.code-content b {
    color: #66e3a4;
}

/* =========================================
   SECTION TITLE
========================================= */

.section-title {
    text-align: center;
    max-width: 700px;
    margin: auto;
}

.section-title > p:first-child {
    color: #6c4df6;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 2px;
}

.section-title h2 {
    font-size: 40px;
    margin: 8px 0 12px;
}

.section-title h2 span {
    color: #6c4df6;
}

.section-title p:last-child {
    color: #777;
}

/* =========================================
   SERVICES
========================================= */

.services-preview {
    padding: 100px 7%;
    background: white;
}

.service-container {
    max-width: 1150px;
    margin: 55px auto 0;

    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 22px;
}

.service-card {
    padding: 35px 25px;
    text-align: center;

    background: #f8f7ff;
    border: 1px solid #eeeaff;
    border-radius: 20px;

    transition: 0.4s;
}

.service-card:hover {
    transform: translateY(-10px);
    border-color: #6c4df6;
    box-shadow: 0 20px 40px rgba(108, 77, 246, 0.12);
}

.service-icon {
    width: 70px;
    height: 70px;

    margin: auto auto 20px;

    display: flex;
    align-items: center;
    justify-content: center;

    font-size: 32px;

    border-radius: 20px;
    background: linear-gradient(135deg, #6c4df6, #00c6ff);
}

.service-card h3 {
    margin-bottom: 12px;
    font-size: 19px;
}

.service-card p {
    color: #666;
    font-size: 14px;
    margin-bottom: 18px;
}

.service-card a {
    color: #6c4df6;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
}

/* =========================================
   ABOUT PREVIEW
========================================= */

.about-preview {
    min-height: 500px;
    padding: 100px 7%;

    display: flex;
    align-items: center;

    background:
        linear-gradient(
            135deg,
            #17152d,
            #292152
        );

    color: white;
}

.about-content {
    max-width: 700px;
    margin-left: 8%;
}

.section-label {
    color: #9c8aff;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 2px;
}

.about-content h2 {
    font-size: 43px;
    line-height: 1.25;
    margin: 12px 0 20px;
}

.about-content h2 span {
    color: #8d7aff;
    display: block;
}

.about-content p:not(.section-label) {
    color: #c5c2d5;
    margin-bottom: 18px;
}

/* =========================================
   CTA
========================================= */

.cta {
    padding: 90px 25px;
    text-align: center;

    background:
        linear-gradient(
            135deg,
            #6c4df6,
            #3926a3
        );

    color: white;
}

.cta h2 {
    font-size: 38px;
    margin-bottom: 10px;
}

.cta p {
    color: #dedaff;
    margin-bottom: 25px;
}

/* =========================================
   FOOTER
========================================= */

.footer {
    padding: 70px 25px 20px;
    background: #151426;
    color: white;
}

.footer-content {
    max-width: 1100px;
    margin: auto;

    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 60px;
}

.footer .logo {
    color: white;
    margin-bottom: 15px;
}

.footer p {
    color: #aaa7b8;
    font-size: 14px;
}

.footer h3 {
    margin-bottom: 15px;
}

.footer-content > div:nth-child(2) {
    display: flex;
    flex-direction: column;
}

.footer-content a {
    color: #aaa7b8;
    text-decoration: none;
    margin-bottom: 7px;
    transition: 0.3s;
}

.footer-content a:hover {
    color: #8d7aff;
    padding-left: 5px;
}

.footer-bottom {
    max-width: 1100px;
    margin: 50px auto 0;
    padding-top: 20px;

    text-align: center;

    border-top: 1px solid #302d43;
}

/* =========================================
   DARK MODE
========================================= */

body.dark-mode {
    background: #10101a;
    color: #f5f5f5;
}

body.dark-mode .header {
    background: #181827;
}

body.dark-mode .nav-links a {
    color: #ddd;
}

body.dark-mode .nav-links a:hover,
body.dark-mode .nav-links a.active {
    color: #9a87ff;
}

body.dark-mode .hero {
    background: #10101a;
}

body.dark-mode .hero-content > p:not(.hero-subtitle) {
    color: #bbb;
}

body.dark-mode .services-preview {
    background: #151522;
}

body.dark-mode .service-card {
    background: #1c1c2b;
    border-color: #29293b;
}

body.dark-mode .service-card p {
    color: #bbb;
}

body.dark-mode .section-title p:last-child {
    color: #bbb;
}

body.dark-mode .secondary-btn {
    background: #1c1c2b;
}

body.dark-mode .theme-btn {
    background: #292744;
}

/* =========================================
   TABLET
========================================= */

@media (max-width: 1000px) {

    .nav-links {
        gap: 12px;
    }

    .nav-links a {
        font-size: 12px;
    }

    .hero {
        grid-template-columns: 1fr;
        text-align: center;
    }

    .hero-content {
        margin: auto;
    }

    .hero-image {
        margin-top: 20px;
    }

    .service-container {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* =========================================
   MOBILE
========================================= */

@media (max-width: 700px) {

    .navbar {
        position: relative;
    }

    .menu-toggle {
        display: block;
    }

    .nav-links {
        display: none;

        position: absolute;
        top: 75px;
        left: 0;

        width: 100%;

        padding: 20px;

        background: white;

        flex-direction: column;
        gap: 15px;

        box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    }

    .nav-links.show {
        display: flex;
    }

    .hero {
        padding: 70px 20px;
        min-height: auto;
    }

    .hero h1 {
        font-size: 38px;
    }

    .hero-content > p:not(.hero-subtitle) {
        font-size: 15px;
    }

    .code-card {
        width: 100%;
        max-width: 400px;
    }

    .services-preview {
        padding: 75px 20px;
    }

    .service-container {
        grid-template-columns: 1fr;
    }

    .about-preview {
        padding: 75px 20px;
    }

    .about-content {
        margin-left: 0;
    }

    .about-content h2 {
        font-size: 34px;
    }

    .cta h2 {
        font-size: 30px;
    }

    .footer-content {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 35px;
    }
}

/* =========================================
   SMALL MOBILE
========================================= */

@media (max-width: 450px) {

    .navbar {
        padding: 15px;
    }

    .logo {
        font-size: 23px;
    }

    .hero h1 {
        font-size: 31px;
    }

    .hero-buttons {
        display: flex;
        flex-direction: column;
        gap: 12px;
    }

    .secondary-btn {
        margin-left: 0;
    }

    .code-content {
        padding: 25px 15px;
        font-size: 13px;
    }

    .section-title h2 {
        font-size: 30px;
    }
}